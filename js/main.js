"use strict";

var datetime = new Date();
var month = datetime.getMonth() + 1;
datetime.setDate(1);
var startDay = datetime.getDay();

window.addEventListener("DOMContentLoaded", function (event) {});